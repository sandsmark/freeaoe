name: CMake

on: [push]

env:
  # Customize the CMake build type here (Release, Debug, RelWithDebInfo, etc.)
  BUILD_TYPE: Release
  VERBOSE: True
    #CC: gcc-9
    #CXX: g++-9

jobs:
  build:
    # We abuse || true a lot to make sure caches are up to date

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest]
        #os: [macOS-latest]

    steps:
    - uses: actions/checkout@v1

    - name: cache vcpkg crap
      uses: actions/cache@v1
      with:
        path: C:\Users\runneradmin\AppData\Local\vcpkg\archives\
        key: whatthefuckvcpkg

    - name: Install SFML
      run: |
        vcpkg update
        vcpkg upgrade --no-dry-run
        vcpkg install sfml:x86-windows-static
        vcpkg integrate install

    - name: fetch submodules
      run: git submodule update --init --recursive

    - name: Configure CMake
      # Use a bash shell so we can use the same syntax for environment variable
      # access regardless of the host operating system
      shell: bash
      run: cmake . -DCMAKE_BUILD_TYPE=$BUILD_TYPE -DENABLE_IPO=True -DCMAKE_BUILD_TYPE=Release -DVC_STATIC=True -G "Visual Studio 16 2019" -A Win32 -DVCPKG_TARGET_TRIPLET=x86-windows-static -DCMAKE_VERBOSE_MAKEFILE:BOOL=ON -DCMAKE_TOOLCHAIN_FILE=C:/vcpkg/scripts/buildsystems/vcpkg.cmake || true

    - name: Build
      shell: bash
      run: cmake --build . --config $BUILD_TYPE || true

        #- name: Test
        #  working-directory: ${{runner.workspace}}/build
        #  shell: bash
        #  # Execute tests defined by the CMake configuration.  
        #  # See https://cmake.org/cmake/help/latest/manual/ctest.1.html for more detail
        #  run: ctest -C $BUILD_TYPE
